import { styled } from "../../styles";

export const HeaderContainer = styled("header", {
    width: "calc(100% - 44px)",
    maxWidth: "1128px",
    display: "flex",
    justifyContent: "space-between",
    padding: "32px 22px"
})